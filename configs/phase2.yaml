# Phase 2 base config (EfficientNetV2-S)
model:
  backbone: "efficientnetv2_s"
  image_size: 224
  pretrained: true
training:
  batch_size: 64
  epochs: 15
  focal_gamma: 2.0
  focal_alpha: 0.25
  optimizer: "AdamW"
  lr_backbone: 5.0e-5
  lr_head: 5.0e-4
  TTA: true
